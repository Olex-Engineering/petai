export interface RealPet {
    id: string;
    imageUri: string;
    name: string;
    wallet: string;
    uri: string;
    description: string;
}

export interface RealPetWeb3 {
    wallet: string;
}